---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"span"> & {
  color?: "default" | "brand" | "success" | "error" | "warning" | "info";
  size?: "sm" | "lg" | "md";
  style?: "default" | "muted";
};

const { class: className, color = "default", style="default", size = "sm", ...attrs } = Astro.props;
---

<span class:list={["badge", color, size, style, className]} {...attrs}>
  <slot />
</span>

<style>
  .badge {
    @apply rounded-md;
    @apply text-sm py-0.5 px-2 leading-none;
    @apply bg-neutral-300 text-neutral-900;
  }

  .badge.md {
    @apply text-base py-1 px-2;
  }

  .badge.lg {
    @apply text-base py-1 px-4;
  }

  .badge.brand {
    @apply bg-brand-300 text-brand-700;
    @apply border border-brand-500;
  }

  .badge.success {
    @apply bg-success-300 text-success-700;
    @apply border border-success-500;
  }

  .badge.error {
    @apply bg-error-300 text-error-700;
    @apply border border-error-500;
  }

  .badge.warning {
    @apply bg-warning-300 text-warning-700;
    @apply border border-warning-500;
  }

  .badge.info {
    @apply bg-info-300 text-info-700;
    @apply border border-info-500;
  }

  .badge.muted {
    @apply !bg-transparent;
  }
</style>
